import { appNavigationRef } from "@navigation/NavigationContainer";

/**
 * @deprecated This navigation reference is deprecated. Use the navigation ref from `@navigation/navigators/rootNavigator` instead.
 */

export const navigator = appNavigationRef;
