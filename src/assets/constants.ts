import { colors } from "./colors";
import { strings } from "./strings";
import { commonStyles } from "./styles";
import { themes } from "./themes";

// C is short term for constants
export const C = {
	themes,
	strings,
	commonStyles,
	colors,
};
