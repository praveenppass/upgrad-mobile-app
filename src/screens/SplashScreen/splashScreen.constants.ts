import { horizontalScale } from "@utils/functions";

export const ANIMATION_CONSTANTS = {
	ANIMATION_DURATION: 200,
	LOGO_CHANGE_DELAY: 400,
	ANIMATION_FOCUS_DURATION: 280,
	START_ANIMATION_VALUE: 1000,
	SCALE_ANIMATION_DURATION: 10000,
};

export const LOGO_ICON_SIZE = {
	width: horizontalScale(200),
	height: horizontalScale(200),
};

export const SVG_SIZE = {
	width: horizontalScale(220),
	height: horizontalScale(240),
};
